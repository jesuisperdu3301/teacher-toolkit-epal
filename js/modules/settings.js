import {packDatabases} from '../core/database.js';import {download} from '../core/ui.js';import {resetState} from '../core/storage.js';
export function renderSettings(app){return `<div class="grid"><section class="card span6 stack"><h3>Ρυθμίσεις</h3><label><p class="label">Θέμα</p><select id="themeSelect"><option value="light" ${app.state.settings.theme==='light'?'selected':''}>Light</option><option value="dark" ${app.state.settings.theme==='dark'?'selected':''}>Dark</option></select></label><label><p class="label">Προεπιλεγμένο μάθημα</p><select id="subjectSelect"><option ${app.state.settings.subject==='Υγιεινή'?'selected':''}>Υγιεινή</option><option ${app.state.settings.subject==='Ανατομία'?'selected':''}>Ανατομία</option></select></label><button class="btn secondary" id="backupBtn">Κατέβασμα backup</button><button class="btn danger" id="resetBtn">Reset τοπικών δεδομένων</button></section><section class="card span6"><h3>Πώς λειτουργεί το auto-save</h3><p class="hint">Το app αποθηκεύει σε LocalStorage του συγκεκριμένου browser. Στο GitHub Pages δεν υπάρχει κοινή βάση δεδομένων. Κάθε συσκευή έχει τα δικά της δεδομένα. Για μεταφορά, χρησιμοποιείς Export/Import .epack.</p><h3>PWA</h3><p class="hint">Με GitHub Pages + manifest + service worker, το app μπορεί να εγκατασταθεί και να δουλεύει offline μετά την πρώτη φόρτωση.</p></section></div>`}
export function bindSettings(app){document.getElementById('themeSelect')?.addEventListener('change',e=>{app.state.settings.theme=e.target.value;app.save();app.render()});document.getElementById('subjectSelect')?.addEventListener('change',e=>{app.state.settings.subject=e.target.value;app.save();app.render()});document.getElementById('backupBtn')?.addEventListener('click',()=>download(JSON.stringify(packDatabases(app.state.databases),null,2),'teacher-toolkit-epal-backup.epack'));document.getElementById('resetBtn')?.addEventListener('click',()=>{if(confirm('Reset; Κάνε πρώτα backup αν χρειάζεται.')){resetState();location.reload()}})}
